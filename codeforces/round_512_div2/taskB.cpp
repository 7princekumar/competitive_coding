#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int area_of_triangle(int x1)

int main(){
    int n, d; cin >> n >> d;
    int m; cin >> m;

    vector<int> X;
    vector<int> Y;
    int x, y;
    for(int i=0; i<n; i++){
        cin >> x >> y;
        X.push_back(x);
        Y.push_back(y);
    }

    vector<int> rX;
    vector<int> rY;
    rX.push_back()
    return 0;
}